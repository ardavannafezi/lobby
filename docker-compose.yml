services:
  lobby:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - lobby_data:/app/data
    restart: unless-stopped

volumes:
  lobby_data:
